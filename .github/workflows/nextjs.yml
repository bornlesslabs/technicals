# Custom workflow for building and deploying a Next.js site to GitHub Pages
name: Deploy Next.js site to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Clean workspace
        run: |
          echo "=== Cleaning potential conflicting files ==="
          rm -rf .next || true
          rm -rf out || true
          rm -rf node_modules || true
          rm -f next.config.js || true
          find . -name "next.config.js" -delete || true
          echo "=== Verifying clean state ==="
          ls -la next.config.* || true
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'yarn'
      
      - name: Install dependencies
        run: yarn install --immutable
      
      - name: Debug and clean config files
        run: |
          echo "=== Before cleanup ==="
          find . -name "next.config.*" -type f || true
          ls -la next.config.* || true
          echo "=== Removing any next.config.js files ==="
          find . -name "next.config.js" -delete || true
          rm -f next.config.js || true
          echo "=== After cleanup ==="
          find . -name "next.config.*" -type f || true
          ls -la next.config.* || true
          echo "=== Verifying our next.config.mjs ==="
          cat next.config.mjs
          echo "=== Creating .nextrc to force mjs config ==="
          echo '{"configFile": "./next.config.mjs"}' > .nextrc
      
      - name: Build Next.js
        env:
          GITHUB_PAGES: true
          NODE_ENV: production
        run: |
          echo "=== Final check before build ==="
          ls -la next.config.* || true
          rm -f next.config.js || true
          echo "=== Checking for any postinstall scripts ==="
          find node_modules -name "*.js" -path "*/postinstall*" || true
          echo "=== Building ==="
          yarn build
          echo "=== Checking if config files appeared during build ==="
          ls -la next.config.* || true
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
